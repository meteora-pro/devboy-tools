# devboy-secret-patterns

Catalogue of secret types — regex, severity, retrieval/rotation hints, optional
liveness probe — shared between the secret store (ADR-020/023), OTLP sanitizer
(#240), and OTEL scan auditor (#242).

Status: scaffolding. See epic
[#247](https://github.com/meteora-pro/devboy-tools/issues/247).
